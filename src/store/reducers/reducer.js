// import * as actionTypes from '../actions/actionTypes';
// import { updateObject } from '../utility';

const initialState = {
  redux: "redux connected"
};

const reducer = (state = initialState, action) => {
  return state;
};

export default reducer;